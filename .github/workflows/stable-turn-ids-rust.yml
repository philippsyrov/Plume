name: stable turn ids rust

on:
  push:
    branches: [codex/stable-transcript-ids]

permissions:
  contents: read

concurrency:
  group: stable-turn-ids-rust-${{ github.ref }}
  cancel-in-progress: true

jobs:
  focused-rust:
    runs-on: macos-15
    timeout-minutes: 15
    env:
      CARGO_BUILD_JOBS: 1

    steps:
      - uses: actions/checkout@v4
      - name: Run stable turn id tests
        run: cargo test --manifest-path src-tauri/Cargo.toml --lib stable_turn_ids -- --test-threads=1
