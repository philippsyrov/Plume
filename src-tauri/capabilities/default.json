{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window. Without a capability file the production bundle blocks the IPC bridge entirely (the dev profile is permissive and hides this). `core:event:default` plus app listener register/remove enables the event listening Plume uses for streamed chat and drag/drop. Keep this list as narrow as possible — broaden only with a comment naming the new caller.",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "core:app:allow-register-listener",
    "core:app:allow-remove-listener"
  ]
}
