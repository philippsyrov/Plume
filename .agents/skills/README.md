# Project-Local Agent References

Use this directory only for repo-specific references that should travel with
the project. Keep global tool-native skills in their native homes:

- Claude: `~/.claude/`
- Codex: `~/.codex/`
- Cursor / other tools: their own plugin systems

Leave this directory minimal unless the project truly needs shared file-based
agent notes.
